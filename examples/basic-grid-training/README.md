# Basic grid training example

Run:

```bash
cargo run -p bricks-ai-app -- demo
```

This creates a small trainer, adds correlations, runs a simple training loop, engraves validated weights, and prints the prediction.
